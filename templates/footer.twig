
{% if ASSET_JS is empty %} 
    {% for js in JS %} 
        {% if js starts with 'http' %}
            <script src="{{ js }}"></script>
        {% else %}
            <script src="dist/js/{{ js }}"></script>
        {% endif %} 
    {% endfor %} 
{% else %} 
    {{ ASSET_JS|raw }} 
{% endif %} 
{{ DEBUGBARRENDER|raw }}
